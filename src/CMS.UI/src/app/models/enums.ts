export enum MenuType {
  FrontEnd = 1,
  Admin,
  Custom,
}

export enum UserType {
  SuperAdmin = 1,
  Admin = 2,
  Member = 3,
}

export enum UserFileType {
  CV = 1,
}

export enum SourceType {
  Blog = 1,
  PhotoGallery,
  VideoGallery,
  Job,
}
