
export default {
    methods: {
        successMessage(message) {
            if (message) {
                // notify({
                //     message: message,
                //     width: 500,
                //     position: "top"
                // }, "success", 3000);
            }
        },
        errorMessage(message) {
            if (message) {
                // notify({
                //     message: message,
                //     width: 500,
                //     position: "top"
                // }, "error", 3000);
            }
        }
    }
}